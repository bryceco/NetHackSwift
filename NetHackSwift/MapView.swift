import SwiftUI
import AppKit

// Placeholder until the tile/ASCII rendering view is ported.
// Replace NSViewRepresentable with a real implementation once MainView exists.
struct MapView: View {
    var body: some View {
        Color(red: 0.098, green: 0.098, blue: 0.098)
    }
}

#Preview {
    MapView()
        .frame(width: 637, height: 216)
}
